django-admin startproject [projectName] : Start a django project, it must be
  ingressed a project name.

** Every command with 'manage.py' it must me invoked whe its find the file

python manage.py migrate : Updates database schema. Manages both apps with
migrations and those without. 

python manage.py runserver : Starts a lightweight Web server for development
and also serves static files.

python manage.py startapp : Create a Django app directory structure for the
given app name in the current directory or optionally in the given directory.

python manage.py makemigrations : Creates new migration(s) for apps.

python manage.py migrate : Updates database schema. Manages both apps with
migrations and those without.

python manage.py createsuperuser : Creates a super user.
